{
  "name": "dynamodb-get-expression-attributes",
  "description": "A simple utility to extract the needed `ExpressionAttributeNames` and `ExpressionAttributeValues` from a DynamoDB query/scan expression.",
  "version": "2.0.0",
  "author": "John Flesch <john@fles.ch> (https://fles.ch/)",
  "bugs": {
    "url": "https://github.com/flesch/dynamodb-get-expression-attributes/issues"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-env": "^1.1.8"
  },
  "homepage": "https://github.com/flesch/dynamodb-get-expression-attributes",
  "keywords": [
    "attribute",
    "documentclient",
    "dynamodb",
    "expression"
  ],
  "license": "MIT",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/flesch/dynamodb-get-expression-attributes.git"
  },
  "scripts": {
    "compile": "mkdir -p dist && babel index.js --out-file dist/index.js",
    "prepublish": "npm run compile",
    "test": "node test.js"
  }
}
